server:
  servlet:
    contextPath: /api
  port: 8999

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://someuri.com/to-test-without-error
  messages:
    basename: i18n/messages
    encoding: utf-8
    fallback-to-system-locale: false
  data:
    mongodb:
      database: test
      uri: mongodb://dev:123@192.168.123.103:27017/scheme-manager-back?authSource=admin
  kafka:
    bootstrap-servers: 192.168.123.22:9094

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 3.3.1

app:
  language: ru
  allowed-equipment-number: 200

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

services:
  address:
    integration-platform: http://192.168.123.101:8200
