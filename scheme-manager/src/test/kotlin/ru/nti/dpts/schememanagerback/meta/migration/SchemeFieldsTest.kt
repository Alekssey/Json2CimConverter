package ru.nti.dpts.schememanagerback.meta.migration

object SchemeFieldsTest {
    const val id = "_id"
    const val nodes = "nodes"

    object Node {
        const val type = "type"
        const val libEquipmentId = "libEquipmentId"
        const val fields = "fields"
        const val substationId = "substationId"
        const val transmissionLineId = "transmissionLineId"
    }
}
